version: '3.8'

networks:
  app-network:
    driver: bridge

services:
  redis:
    image: redis:7-alpine
    restart: always                  # <--- Added
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: '256M'

  api:
    build: ./api
    restart: always                  # <--- Added
    ports:
      - "8000:8000"
    networks:
      - app-network
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_URL=${REDIS_URL}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: '512M'

  worker:
    build: ./worker
    restart: always                  # <--- Added
    networks:
      - app-network
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - REDIS_URL=${REDIS_URL}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: '512M'

  frontend:
    build: ./frontend
    restart: always                  # <--- Added
    ports:
      - "3000:3000"
    networks:
      - app-network
    depends_on:
      api:
        condition: service_healthy
    environment:
      - API_URL=${API_URL}
    deploy:                          # <--- Added  limits block
      resources:
        limits:
          cpus: '0.5'
          memory: '256M'